import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imageContain: {
    width: '30%',
    gap: 8,
    marginBottom: 10,
  },

  imageBorder: {
    borderRadius: 8,
  },
  imageDescription: {
    fontStyle: 'italic',
    paddingBottom: 8,
    textTransform: 'capitalize',
  },

  positionItemInTheScreen: {
    gap: 12,
    justifyContent: 'center',
    padding: 12,
  },
});
